---
title: About Us

# true if the page is included in the menu
listed: true

# true if the page is published
published: true
---
{% extends parent %}

{% block main %}

	<div class="row contentrow">
	  <div class="col-sm-10 col-sm-offset-1 col-xs-12 content-text">
	  	<p>Reclaimed began when the Lord laid it on the hearts of co-founders Sheetal Agrawal and Christy Chermak. They quickly realized that the Lord was raising up an army for the Lord in the fight against sex trafficking. Joined by other freedom fighters, Reclaimed began meeting in July of 2013. Supported by Watermark Community Church, Reclaimed became ministry partners with NewFriendsNewLife and JSL in November of 2014.</p>
	  </div>
	</div>

	<div class="row">
	<div class="col-md-offset-2 col-md-8 col-xs-offset-1 col-xs-10">
	{% for person in contents.people  %}
		<div class="row contentrow">
			<div class="col-md-6 col-xs-12">
				{% if person.image.url %}
					<img src="{{ person.image.url | resize: '300x' }}" alt="{{ person.name }}">
				{% endif %}
			</div>
			<div class="hidden-xs col-xs-12">
				<h3>{{ person.name }}</h3>
				<div class="content-text">
					{{ person.blurb }}
				</div>
			</div>
			<div class="hidden-sm hidden-md hidden-lg col-xs-12" style="text-align:center;">
				<h3>{{ person.name }}</h3>
				<div class="content-text">
					{{ person.blurb }}
				</div>
			</div>
		</div>
	{% endfor %}
	</div>
	</div>

	<div class="row">
		<div class="col-md-8 col-md-offset-2">
			Website Credit: Gordon Burgett and Kristin Mercer
		</div>
	</div>

{% endblock %}
